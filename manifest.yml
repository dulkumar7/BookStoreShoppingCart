applications:
- name: shopping-cart
  path: target/bookstore.war
  routes:
    - route: book-store-jenkins-pipline-setup.cfapps.io
  env:
    WELCOME_MESSAGE: book store
  services:
    service: book-store-db
